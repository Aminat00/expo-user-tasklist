const assets = "../../assets";
export const fontsMap = {
	Helvetica: require(`${assets}/fonts/HelveticaNowDisplay-Black.ttf`),
	Inter: require(`${assets}/fonts/InterTight-Black.ttf`),
};

export const profileImg = require(`${assets}/images/profile-img.png`);
export const icon = require(`${assets}/favicon.png`);
export const headerBg = require(`${assets}/header-bg-image.png`);
